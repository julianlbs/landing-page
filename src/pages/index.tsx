import type { NextPage } from "next";
import Layout from "../components/layout/layout";
// import Head from "next/head";

const Home: NextPage = () => {
	return (
		<Layout>
			<main>
				<div>Hello World!</div>
			</main>
		</Layout>
	);
};

export default Home;
